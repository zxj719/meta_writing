# meta_writing 项目会话记录

**日期：** 2026-03-26 ~ 2026-03-28
**参与者：** 用户 + Claude (通过 gstack `/office-hours` 和 `/plan-eng-review` 技能)

---

## 一、项目概述

**meta_writing** 是一个多 Agent 中文网络小说自动生成系统。核心目标：通过多个专业化 AI Agent 协作，生成百万字级别的长篇网络小说，同时保持人物、剧情、风格、主题的一致性。

**质量标准：** 网文读者无法分辨是 AI 生成的。
**目标平台：** 番茄小说、起点中文网等网络小说平台。

---

## 二、Office Hours 头脑风暴阶段

### 2.1 问题定义

当前 LLM 在单章级别可以生成合格的网文散文，但在 100+ 章、百万字的规模下完全无法维持一致性：
- 人物声音模糊
- 前期铺垫的伏笔后期遗忘
- 主题一致性漂移
- 世界观规则自相矛盾

番茄小说上 AI 生成内容同比激增 13 倍，但全是粗制滥造，没有任何方案解决一致性问题。

### 2.2 三种方案对比

| 方案 | 描述 | 工作量 | 风险 | 完整度 |
|------|------|--------|------|--------|
| A: Story Bible + 流水线 | 线性流程，简单 Python 脚本编排 | S | 低 | 6/10 |
| **B: 多 Agent 集成 + 共享记忆** | 专业化 Agent + 共享 Story Bible 记忆层 | M | 中 | **9/10** |
| C: 生活世界模拟 | 角色作为独立 Agent，涌现式叙事 | L | 高 | 7/10 |

**最终选择：方案 B**——架构完整、可扩展，是通往方案 C（10x 愿景：活的故事世界）的自然演进路径。

### 2.3 Agent 架构（5 个专业 Agent + 1 个编排器）

1. **Orchestrator（编排器）**——管理整体工作流、路由任务、维护流水线状态
2. **Planner Agent（策划）**——生成下一章大纲，提供 2-3 个剧情分支选项，人类选择走向
3. **Writer Agent（写手）**——根据选定大纲 + Story Bible + 近期章节上下文，生成完整章节散文
4. **Continuity Agent（连续性检查）**——验证章节与 Story Bible 的一致性：人物状态、关系、时间线、世界规则、伏笔审计
5. **Style Agent（风格检查）**——叙事声音、POV 一致性、网文惯例、AI 味检测
6. **Theme Agent（主题检查）**——核心主题追踪、角色弧线对齐、价值观一致性

### 2.4 写作方法论集成

系统将成熟的网文创作方法论内嵌到各个 Agent 的行为中（参考 `skills_novel-writing-expert-v2.md`）：

| 方法论 | 对应 Agent |
|--------|-----------|
| 三幕式结构、爽点分布、钩子布局、五层世界揭示 | Planner |
| 五感描写、对话四功能、长短句节奏 | Writer |
| 角色动机三角验证、行为逻辑("为什么?")、五阶段成长弧线 | Continuity |
| 节奏公式、钩子强制、对话质量、AI 味检测 | Style |
| 五层世界架构对齐、角色弧线阶段追踪、核心主题一致性 | Theme |

**七大核心框架：**
1. **一句话核心（Hook）**——故事开始前必须有一句话概括核心吸引力
2. **五层世界架构法**——表层世界 → 规则层 → 禁忌层 → 真相层 → 本质层
3. **角色核心三角形**——欲望 × 能力 × 阻碍
4. **动机类型矩阵**——生存/情感/利益/使命/好奇心
5. **爽点布局**——小爽点（章内）/ 中爽点（副本中期）/ 大爽点（弧线高潮）
6. **钩子技术**——悬念/冲突/情感/反转四种钩子
7. **节奏公式**——紧张 → 短暂缓解 → 更大紧张 → 高潮 → 余韵

### 2.5 共享记忆层：Story Bible

**结构化状态（YAML 文件）：**
- `story_core.yaml`——一句话核心、体裁、目标爽点类型、五层世界架构
- `characters/`——每个角色一个文件：外貌、性格、知识状态、情感状态、关系、目标、位置、核心三角、动机类型、成长弧线阶段
- `timeline.yaml`——有序事件 + 章节引用
- `world_rules.yaml`——魔法体系、科技水平、地理、社会结构
- `foreshadowing.yaml`——伏笔设置/回收对，状态：`planted` | `reinforced` | `paid_off` | `abandoned`
- `pacing.yaml`——爽点时间表、钩子布局日志、张力曲线追踪
- `chapter_summaries/`——每章结构化摘要

**向量存储（语义检索）：**
- ChromaDB + BGE-M3（本地中文优化嵌入）
- 所有生成章节按场景分块（~500-1000 字/块）嵌入
- 支持"找到 X 发生的那一章"查询

### 2.6 每章工作流

```
1. Orchestrator 加载当前 Story Bible 状态
2. Planner Agent 生成 2-3 个剧情分支选项
3. 人类选择分支（可修改/组合）
4. Writer Agent 起草完整章节
5. 并行审查：Continuity + Style + Theme Agent
6. 发现问题 → Writer 修改（最多 3 轮迭代）
   - 3 轮后仍未解决 → 上报人类，展示剩余问题的 diff
   - 审查 Agent 反馈冲突 → Orchestrator 展示双方观点，人类裁决
7. 人类审查最终稿（批准 / 退回附注释 / 行内编辑）
8. Orchestrator 原子提交：章节文本 + Story Bible 更新 + 伏笔更新 + 章节摘要
9. 重复
```

---

## 三、工程评审阶段

### 3.1 七项关键架构决策

| # | 决策项 | 选择 | 理由 |
|---|--------|------|------|
| 1 | MVP 范围 | **3 个 Agent（Planner + Writer + Continuity）** | 先验证核心一致性循环，Style/Theme 推迟到 Post-MVP |
| 2 | Agent 框架 | **Claude Agent SDK** | 与 Claude 更紧密集成，原生工具使用支持 |
| 3 | 嵌入方案 | **BGE-M3 本地嵌入** | 无需外部 API，RTX 5070（12GB VRAM）+ i5 完全可行（模型仅需 ~2.2GB VRAM） |
| 4 | 状态同步 | **LLM 提取 + 人工确认** | 每章提交后 LLM 提取状态变更，人类审核后才写入 Story Bible |
| 5 | 项目结构 | **扁平模块结构** | 简洁明了，MVP 阶段避免过度抽象 |
| 6 | 测试策略 | **Mock LLM 做单元测试** | pytest + pytest-asyncio，真实 LLM 测试标记 `@pytest.mark.integration` 手动运行 |
| 7 | LLM 分层 | **Writer 用 Sonnet 起草** | 成本从 $800-1200（全 Opus）降至 ~$300-500/本，Planner 仍用 Opus |

### 3.2 独立审查（Outside Voice）发现

引入独立 Claude 子 Agent 做对抗性审查，提出 10 项发现：

**最关键风险：** 状态提取漂移——随着章节增多，LLM 提取的 Story Bible 更新可能逐渐偏离实际内容。

**解决方案：**
- 加入 **Bible 重建工具**——可从所有原始章节重新构建完整 Story Bible（灾难恢复机制）
- 人工确认每次状态提取结果

### 3.3 项目结构

```
meta_writing/
├── pyproject.toml              # anthropic, claude-agent-sdk, chromadb,
│                               # sentence-transformers, pyyaml, rich
├── meta_writing/
│   ├── __init__.py
│   ├── cli.py                  # CLI 入口（Rich TUI）
│   ├── orchestrator.py         # 流水线控制器 + 状态提交
│   ├── agents/
│   │   ├── __init__.py
│   │   ├── planner.py          # 剧情分支生成（Opus）
│   │   ├── writer.py           # 章节散文生成（Sonnet）
│   │   └── continuity.py       # 一致性验证（Sonnet）
│   ├── story_bible/
│   │   ├── __init__.py
│   │   ├── schema.py           # Pydantic 模型（YAML 验证）
│   │   ├── loader.py           # 加载/保存 YAML 状态（懒加载）
│   │   └── compressor.py       # 上下文预算管理
│   └── vector_store/
│       ├── __init__.py
│       └── store.py            # ChromaDB + BGE-M3 封装
├── story_data/                 # Story Bible YAML 文件（git 跟踪）
│   ├── story_core.yaml
│   ├── characters/
│   ├── timeline.yaml
│   ├── world_rules.yaml
│   ├── foreshadowing.yaml
│   └── chapter_summaries/
├── chapters/                   # 生成的章节文本（git 跟踪）
└── tests/
    ├── test_orchestrator.py
    ├── test_planner.py
    ├── test_writer.py
    ├── test_continuity.py
    ├── test_story_bible.py
    └── test_vector_store.py
```

### 3.4 测试计划摘要

**单元测试（Mock LLM）——42 条测试路径：**

| 模块 | 覆盖点 |
|------|--------|
| Orchestrator | 完整流程、修订循环（最多 3 轮）、修订耗尽上报、API 故障恢复、状态提取、原子提交/回滚 |
| Planner | 正常分支生成、伏笔到期提醒、API 超时重试 |
| Writer | 正常章节生成、定向修订、上下文溢出压缩 |
| Continuity | 清洁通过、角色状态矛盾、时间线矛盾、世界规则违反、伏笔老化、关系矛盾 |
| Story Bible | 角色 YAML 解析/验证、StoryCore 解析、伏笔对解析、加载器往返、压缩器分级 |
| Vector Store | 添加章节块、修订后重嵌入、查询返回/空结果 |

**端到端测试（手动，真实 LLM）：**
- 完整流水线：初始化 → 生成第 1 章 → 审查 → 提交 → 生成第 2 章
- 修订循环：Writer 产生矛盾 → Continuity 捕获 → Writer 修复
- 3 章弧线：第 1 章植入伏笔，第 2-3 章追踪
- Story Bible 生命周期：空 → 填充 → 跨章节演化

### 3.5 上下文窗口预算（200K 上下文模型）

| 内容 | Token 预算 |
|------|-----------|
| Story Bible 状态（活跃角色 + 近期时间线 + 相关伏笔） | ~15K |
| 近 3 章全文 | ~30K |
| 大纲 | ~3K |
| 生成输出 | ~10K |
| 缓冲/系统 | ~142K |

超过 12+ 活跃角色时，次要角色压缩为 2-3 句摘要。

### 3.6 成本估算

| 配置 | 每本 100 章小说成本 |
|------|-------------------|
| 全 Opus | ~$800-1,200 |
| **Planner Opus + Writer/Reviewers Sonnet** | **~$300-500** |
| 未来 DeepSeek/Qwen 替代 Writer | ~$50-100（待验证） |

### 3.7 失败模式与恢复

- **API 中途故障：** 指数退避重试（3 次），持久化部分状态，允许从检查点恢复
- **Story Bible 损坏：** git 回滚到最后已知良好提交，每章提交是原子的
- **人工编辑引入新信息：** Orchestrator 触发 Story Bible 调和——Continuity Agent 重新验证并提取新实体/事件
- **追溯修正（Retcon）：** 人类标记 retcon，更新 Story Bible，生成 retcon 日志供后续章节参考
- **限流保护：** 审查 Agent 错开 1 秒延迟，Orchestrator 实现请求队列

---

## 四、Post-MVP 待办事项

1. **30 章压力测试**——验证长程一致性、伏笔老化、Story Bible 提取漂移（预计 ~$100-150，~1 天）
2. **Style Agent + Theme Agent 实现**——添加风格和主题质量检查（依赖 30 章测试通过）
3. **评估 DeepSeek/Qwen 作为 Writer Agent**——对比中文散文质量、一致性和成本（可能将成本降至 $50-100）

---

## 五、成功标准

- **10 章弧线测试：** 网文读者盲测评价为"可能是人类写的"
- **一致性分数：** Continuity Agent 在 10 章中检测到零矛盾
- **伏笔回收：** 第 1-3 章植入的所有伏笔在第 10 章前得到回收或强化
- **风格一致性：** Style Agent 检测不到跨弧线的声音漂移
- **生成速度：** 单章完整 API/处理时间 < 21 分钟（Planner ~3 分钟, Writer ~5 分钟, 并行审查 ~3 分钟, 修订 ~5 分钟×2 轮）

---

## 六、关键产出文件

| 文件 | 说明 |
|------|------|
| `Administrator-unknown-design-20260326-230846.md` | 核心设计文档（已批准） |
| `Administrator-unknown-eng-review-test-plan-20260327-2245.md` | 测试计划 |
| `TODOS.md` | Post-MVP 待办事项 |
| `skills_novel-writing-expert-v2.md` | 网文创作方法论参考（中文） |

---

## 七、当前状态

**工程评审已通过（CLEARED）。** 架构、数据流、测试覆盖和性能指标已锁定。尚未开始编码。下一步：开始实现（可运行 `/ship` 启动）。
